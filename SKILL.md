---
name: 深度文章分析
name_cn: 深度文章分析
author: 小巷老张
description: |
  深度阅读分析工具（原名 wechat-article-analyzer）。对任何网页内容（公众号、新闻、博客、研报、知乎、论坛、播客文字稿等）
  执行系统化分析：模板驱动执行（分析模式/梳理总结模式双模切换）、营养度评级、核心洞见提取、
  数据联网验证、论证逻辑审视、视角补全、结构化输出。支持 quick/standard/deep 三档深度，内置财经文章专项模式。
  触发词：深度文章分析、分析这篇文章、分析这个链接、深度阅读、帮我分析、帮我读一下、核查这篇、
  分析公众号文章、分析推文 + 链接；梳理总结、学习吸收、帮我整理、学习笔记、播客总结、
  播客笔记 + 链接/文本；或用户提供任何网页 URL 并要求分析/审查/阅读时。
agent_created: true
---
# 深度阅读分析大师

基于唐嘉序的 WorkBuddy 分析公众号文章完整方法论，扩展适配所有类型网页内容的系统化深度分析。

## 触发条件

当用户提供任何网页链接并要求分析、审查、核查、深度阅读、帮我读时触发。包括但不限于：
- 公众号文章（`mp.weixin.qq.com`）
- 媒体新闻（`news.qq.com`, `36kr.com`, 各新闻门户）
- 博客/个人网站
- 研报/行业报告
- 知乎回答/专栏
- 论坛帖子
- 播客文字稿（粘贴全文、播客平台链接如 `xiaoyuzhoufm.com`/`qingting.fm`/`ximalaya.com`，或文本含时间戳 `00:15:30`、说话人标签「主播：」「嘉宾：」等口语化特征）

也适用于用户直接粘贴全文文本（无链接的场景）。

## 内容源适配

不同内容源的分析侧重自动调整：

| 来源类型 | 检测信号 | 分析侧重 |
|----------|----------|----------|
| 公众号文章 | `mp.weixin.qq.com` | 事实核查 + 视角补全（公众号单作者视角） |
| 媒体新闻 | `news.`, `36kr`, `caixin`, `thepaper` 等 | 事实核查权重提高 + 判断报道立场 |
| 博客/专栏 | 个人域名、`zhuanlan.zhihu.com` | 逻辑链审查 + 观点独特性评估 |
| 研报/报告 | PDF、`research`、`report` 关键词 | 财经专项模式 + 数据交叉验证 |
| 知乎回答 | `zhihu.com/question` | 多视角天然存在，聚焦该回答的论证质量 |
| 论坛帖子 | `bbs`, `tieba`, `v2ex` 等 | 营养度预判较低，默认 quick 或跳过 |
| 播客文字稿 | 口语化、时间戳、说话人标签、播客平台URL | 观点输出型，按标准流程处理。用户要求梳理总结时切模板5 |
| 付费/需登录内容 | WebFetch 失败 | 要求用户粘贴全文后按博客/专栏处理 |

## 核心原则

- AI 的角色是「学习伙伴 + 验证者」——先帮你汲取文章的洞见，再帮你检验洞见的可靠性。汲取在前，验证在后。
- 核心价值在第一层（这篇文章值得你记住什么）和第二层（这些说法可靠吗），第一层比第二层更重要
- 对文章保持独立态度，不讨好、不附和、不过度礼貌
- 营养度 < 2 星的文章跳过事实核查和逻辑审查，但视角补全仍做
- **读感目标**：让用户读完后觉得「学到了东西」而非「这篇文章被审判了」

## 参数

| 参数 | 必填 | 默认值 | 说明 |
|------|------|--------|------|
| url | 是 | - | 公众号文章链接，或粘贴全文文本 |
| depth | 否 | standard | quick / standard / deep |
| topic | 否 | auto | 文章主题：auto / finance / tech / society / general |

`topic=auto` 时从文章内容自动判断主题。判断规则：文中出现「营收/毛利率/PE/净利润/估值/股价」等关键词 → finance；「API/架构/模型/算法」→ tech；「政策/社会/教育/医疗」→ society。

## 模板选择矩阵

模板是主执行指令。不支持 depth 和 topic 的任意组合——只允许以下有效组合：

| depth | topic | 使用模板 | 说明 |
|-------|-------|---------|------|
| quick | 任意 | **模板1** | 快速模式不叠加财经专项，财经专项内容放到「明显问题」里提一句 |
| standard | auto/general | **模板5** | 默认梳理总结模式（学习吸收） |
| standard | finance | **模板5 + 模板3** | 梳理总结 + 叠加财经专项检查 |
| standard | tech/society | **模板5** | 梳理总结模式，按主题侧重调整 |
| deep | auto/general | **模板5 + 思维链** | 深度模式自动启用思维链 |
| deep | finance | **模板5 + 模板3 + 思维链** | 深度财经三合一 |
| - | 两篇文章 | **模板4** | 对比分析，不参与 depth/topic 矩阵 |
| - | 用户要求分析/核查/深度阅读 | **模板2** | 分析审查模式，需明确触发 |

**决策流程（按顺序命中即停）**：

0. 用户明确要求分析/核查/深度阅读/按模板2 → 模板2
1. 两篇文章链接 → 模板4
2. depth=quick → 模板1
3. depth=standard + topic=finance → 模板5+3
4. depth=deep + topic=finance → 模板5+3+思维链
5. depth=deep + topic≠finance → 模板5+思维链
6. 默认 → 模板5

## 模板分档策略

| 板块 | quick | standard | deep |
|------|:---:|:---:|:---:|
| 一、营养度 + 标签 | ✅ | ✅ | ✅ |
| 二、内容提要 | ✅ 3 条 | ✅ ≥3 条 | ✅ ≥3 条 |
| 三、逐层拆解 | ❌ | ✅ ≥3/层 | ✅ ≥3/层 |
| 四、核心洞见 | ✅ 3 条 | ✅ ≥5 条 | ✅ ≥5 条 + 交叉验证 |
| 五、反共识 | ✅ 1-2 个 | ✅ ≥5 个 | ✅ ≥5 个 |
| 六、金句 | ✅ 3 句 | ✅ ≥5 句 | ✅ ≥5 句 |
| 七、关键术语 | ❌ | ✅ ≥5 个 | ✅ ≥5 个 |
| 八、总结归纳 | ❌ | ✅ ≥5 条 | ✅ ≥5 条 |
| 九、读后反思 | ✅ 2 个 | ✅ ≥3 个 | ✅ ≥3 个 + 思维链 |
| 十、读后行动 | ✅ 1 条 | ✅ 1-3 条 | ✅ 1-3 条 |

**营养度分支**（仅在 standard/deep 模式生效）：≤2 星缩略——板块 2/3/4/7 各减半，跳板块 5/6。

## 执行流程

模板是主指令。以下为执行时的操作步骤。

### 第零步：内容抓取、来源识别与完整性检查

1. 使用 WebFetch 抓取文章内容。如果没有 URL（用户粘贴文本），直接进入步骤 3
2. 如果 WebFetch 失败（付费文章/需登录），要求用户粘贴全文
3. **来源识别**：根据 URL 域名或文本特征自动判断内容来源类型（参照「内容源适配」表）
4. **截断检测**：检查返回内容结尾是否有突然断句、不完整段落。如有截断信号，提示用户「内容可能不完整，建议粘贴全文」
5. 提取元信息：标题、作者/来源、发布日期（如果能获取）
6. 判断文章类型：信息汇总型 / 观点输出型 / 叙事故事型
7. **长度自检**：估算文章字数。如果 > 8000 字，标注长文，事实核查上限调整为 15 条
8. 根据来源类型调整后续分析侧重（如媒体新闻加大事实核查力度，播客文字稿按观点输出型处理）

### 第一步：营养度评级 + 元数据标注

1. 按五星标准评估文章增量价值，2-3 段详细理由
2. 提取 3-5 个标签（关键词），标注来源类型
3. 根据营养度决定后续执行策略（参照分档策略表）

### 第二步：逐层拆解

将文章按「开始→发展→结论」三层拆开。每组 ≥3 个要点。这是理解文章结构的基础，也为核心洞见提供上下文。

### 第三步：提炼核心洞见 + 金句 + 术语

1. 识别 ≥5 条核心洞见，每条融合主张+亮点+验证注脚
2. 联网验证关键数据（WebSearch），上限 10 条
3. 提取 ≥5 句金句（原文引用）
4. 提取 ≥5 个关键术语（一句话解释）

### 第四步：反共识 + 总结 + 反思 + 行动

1. 挖掘反共识观点（≥5 个，不硬凑）
2. 生成总结归纳（≥5 条，序号）
3. 生成读后反思（≥3 个问题）
4. 生成读后行动（1-3 条）

### 第五步：输出并保存（四格式自动转换：MD → HTML → DOCX → PDF）

按「输出格式」中对应类型的报告结构生成文件保存到本地。展示给用户，等待确认后上传 IMA。

**输出流程**（按顺序执行）：

1. **.md（主报告）**：按模板结构生成 Markdown 报告，自动保存到输出目录。
2. **.html（网页版）**：调用 `md2all.py <输入.md>` 生成同名 .html 文件。包含完整CSS排版（封面页、响应式布局、表格/代码/引用块样式），可直接浏览器打开。
3. **.docx（Word版）**：调用 `md2docx.py` 将**完整的 .md 报告**转为 .docx 文件。适配 Word/WPS，微信可直接发送打开。排版包含：主题色标题、加粗/代码/引用块、斑马纹表格、代码块灰底等。**注意**：必须从完整的 .md 报告转换，不可手工精简内容。
4. **.pdf（打印版）**：由 `md2all.py` 一并生成，通过 Playwright（Chromium 浏览器引擎）渲染 HTML → PDF。排版与 HTML 完全一致（含封面页、页眉页脚、表格/代码样式），质量等同浏览器打印。

**依赖检查**：
- python3（managed Python）已预装 python-docx、markdown、playwright 库，Chromium 浏览器引擎已安装。
- 所有转换脚本存放在 `C:\Users\zhang\.workbuddy\skills\深度文章分析\scripts\` 目录下（skill 自包含，脱离外部目录可运行）。
- 如果 PDF 未生成，执行 `playwright install chromium` 安装浏览器引擎。

所有输出文件在同一目录，命名与 .md 报告一致（仅扩展名不同，共 4 个文件：.md + .html + .docx + .pdf）。

所有输出文件在同一目录，命名与 .md 报告一致（仅扩展名不同，共 4 个文件：.md + .html + .docx + .pdf）。

---

## 财经文章专项模式（topic=finance）

通过模板3叠加执行。额外检查项：

1. **数据专项核查**：估值、营收、增长率等核心数字联网核对，区分「事实数据」和「预测数据」
2. **投资逻辑审查**：相关性≠因果性、行业周期遗漏、选择性使用数据、假设均值回归
3. **OEQ 框架审视**：
   - O（运营）：公司执行力、管理效率
   - E（经济）：行业供需、定价权、竞争格局
   - Q（质量）：管理层品质、护城河深度、企业文化
4. **前提条件验证**：如果文章结论成立，需要满足哪些前提？这些前提当前满足吗？
5. **声明**：不荐股，不给出买卖建议，仅做分析和风险提示。

## 标注速查

| 标签 | 含义 | 触发条件 |
|:--:|------|------|
| ✅ | 真实 | 2+ 独立可靠来源一致确认 |
| ❌ | 虚假 | 2+ 独立可靠来源一致反驳 |
| ⚡ | 数据偏差 | 搜到但偏差 > 20% |
| ⚠️ | 无法验证 | ≤1 个来源或来源不可靠 |
| 🔍 | 截断风险 | WebFetch 内容疑似不完整 |

## 输出格式

### 文件名清洗

生成文件名前，将文章标题中的非法字符替换为 `-`：
```
:\/?*"<>| → -
连续多个 - 合并为一个
首尾 - 去除
```

最终命名：`YYYY-MM-DD_{清洗后标题}.md`

### 报告结构

**文章/网页类**（模板1/2/3/4）：10 板块结构。核心思路：先拆解再提炼，验证数据是附注不是主角。

```markdown
# {文章标题} — 深度阅读分析

**分析日期**：{date}
**分析深度**：{quick|standard|deep}{ + 财经专项}
**来源类型**：{公众号/媒体新闻/博客/研报/知乎/播客稿/其他}
**作者/来源**：{author}
**标签**：{3-5 个关键词}
**链接**：{url}

---

## 一、营养度：{stars}

{2-3段。第一段讲价值。第二段讲为什么没到更高评级。}

## 二、内容提要

{序号形式，≥3 条，每条一句话。提炼文章最关键的信息或论点，不是流水账。

1. ...
2. ...
3. ...
...}

## 三、逐层拆解

### 开始
{≥3 个要点：文章从什么问题/背景切入？提出了什么核心命题？}
### 发展
{≥3 个要点：用什么方法/论据/案例展开论证？视角切换？数据引用？}
### 结论
{≥3 个要点：最终的结论、解决方案或未回答问题是什么？}

## 四、核心洞见

{≥5 条，全文最重要的部分。每条融合三个元素：
- 作者的主张（原文说了什么）
- 分析的亮点（为什么这条值得记住——视角独特？连接了知识？反直觉？）
- 数据验证注脚（对涉及的关键数据顺手联网核对 ✅/⚠️/⚡）
- 格式：段落叙事，三条元素自然融合，不写"主张→验证→✅"}

## 五、反共识挖掘

{≥5 个（标准模式）或 ≥3 个（标准模式下降），不硬凑：
- 文章中与主流认知相悖的观点
- 少见的、容易被忽视的论点
- 挑战行业共识的数据或案例
- 如果文章本身是共识性内容，标注「本文未发现明显反共识观点」并说明为什么}

## 六、金句

{≥5 句（标准模式）或 ≥3 句（标准模式下降），原文引用：
1. "..." —— 为什么选这句
2. "..." —— 为什么选这句
...}

## 七、关键术语

{≥5 个文章中引入或涉及的专业术语/概念，每个一句话解释：
1. **术语A**：...
2. **术语B**：...
...}

## 八、总结归纳

{序号形式，≥5 条。综合以上分析，精炼捕捉文章的精华：
1. ...
2. ...
...}

## 九、读后反思

{≥3 个问题，基于文章内容激发进一步思考：
1. ...
2. ...
3. ...
（不是「找这个信息源看XX」——那是下一步做的事。这一步是让你停下来想。）}

## 十、读后行动

{1-3 条具体可操作的建议。不是「多思考XX」，是「去YY看ZZ」。}

---
*分析工具：WorkBuddy 深度阅读分析大师*
```

**播客/梳理总结类**（模板5）：10 板块，侧重学习吸收 + 保留完整营养度评级和事实核查。报告结构由模板5定义，关键特征：
- 元数据区新增：主播/嘉宾、内容类型、播出平台（播客场景）
- 营养度评级和事实核查标准与模板2一致
- 新增「案例与故事」「可落地的方法」「延伸推荐」「我的思考」板块
- 总结含营养度复核 + 个人评分（1-10）+ 推荐理由

### 输出位置

1. **本地文件（.md + .html + .docx + .pdf）**：自动保存到 **当前任务工作区目录下新建的 `article-analysis\` 子目录**（即 `{当前工作区}\article-analysis\`，用动态 cwd 获取，不写死任何绝对路径）。
   如果目录不存在，先创建。同时生成同名 .html（网页版，浏览器直接打开）、.docx（适配 Word/WPS，微信可直接发送）、.pdf（打印版，排版与 HTML 一致）。
2. **IMA 知识库**：报告生成后，先展示用户确认。用户说「传」「上传」「存到IMA」等确认指令后再执行上传流程。上传时仅上传 .md 文件到知识库。目标文件夹：「AI学习 > 实用推文」

## 提示词模板（运行时注入）

以下五个模板是执行时注入到上下文的主指令。模板中的 `{}` 为运行时变量占位。

### 模板1：快速分析

```
快速读完这篇文章，给我精炼版分析。控制在 500 字以内。

板块（缩略执行）：
1. 营养度：1-5星，一句话理由 + 标签 3 个
2. 内容提要：3 条
3. 核心洞见：3 条（融合主张+亮点，不单独做验证）
4. 反共识：1-2 个（有就说，没有跳过）
5. 金句：3 句
6. 读后反思：2 个问题
7. 读后行动：1 条

跳过的板块：逐层拆解、关键术语、总结归纳
```

### 模板2：深度阅读分析（默认，10板块全量）

```
你的角色是学习伙伴 + 验证者。帮我从这篇文章里汲取知识，顺手告诉我哪里不可靠。

输出结构（10 板块）：

1. 营养度：1-5星，2-3段。如果 ≤2星，缩略执行（板块2/3/4/7各减半，跳板块5/6/8）

2. 内容提要：≥3 条，序号，每条一句话。不是流水账，是提炼最关键的信息或论点。

3. 逐层拆解（≥3 点/层）：
   - 开始：文章从什么切入？核心命题是什么？
   - 发展：用什么方法/论据/案例展开？叙事结构？
   - 结论：最终的结论或未回答的问题？

4. 核心洞见（≥5 条）——全文主角。每条融合三段：
   - 作者的主张（原文说了什么）
   - 亮点点拨（为什么值得记住——视角独特？连接知识？反直觉？）
   - 数据验证注脚（关键数据联网核对 ✅/⚠️/⚡，上限 10 条）
   格式：完整段落叙事，不拆成"主张→验证→✅"

5. 反共识挖掘（≥5 个）：文章中与主流认知相悖的、少见的、容易被忽视的论点。
   不硬凑。如果文章是共识性内容，标注并说明原因。

6. 金句（≥5 句）：原文引用 + 一句说明为什么选它。

7. 关键术语（≥5 个）：专业术语/概念，每个一句话解释。

8. 总结归纳（≥5 条，序号）：综合以上的精炼总结。

9. 读后反思（≥3 个问题）：基于文章内容让你停下来想的问题。
   （不是行动建议——行动在下一步。）

10. 读后行动（1-3 条）：具体可操作。去 YY 看 ZZ，不是「多思考 XX」。

读感目标：读完后觉得「学到了东西」。
直接坦诚，不需要对文章保持礼貌。有问题直接说。
```

### 模板3：财经文章专项

```
这篇文章涉及财经/投资分析，在标准分析基础上增加以下专项检查：

1. 数据专项核查：
   - 文章引用的估值、营收、增长率、市场份额等核心数字，联网交叉核对
   - 区分"事实数据"和"预测数据"，后者标注不确定性

2. 投资逻辑审查，逐项检查：
   - 是否把相关性当因果性？
   - 是否忽略了行业周期？
   - 是否存在选择性使用数据（cherry-picking）？
   - 是否假设均值回归而没有证明？
   - 是否忽略了竞争格局变化？

3. OEQ 框架审视：
   - 运营层面（Operation）：公司执行力、管理效率
   - 经济层面（Economics）：行业供需、定价权、竞争格局
   - 质量层面（Quality）：管理层品质、护城河、企业文化

4. 前提条件验证：
   - 如果文章的核心结论成立，需要满足哪些前提条件？
   - 这些前提条件当前是否满足？有哪些正在弱化？

注意：仅做分析和风险提示，不荐股，不给出买卖建议。
```

### 模板4：观点对比分析

```
我有两篇关于同一主题的文章，请对比分析：

文章A：[链接/标题A]
文章B：[链接/标题B]

1. 分别概括两篇的核心观点（各一句话）
2. 核心分歧：两篇文章在最根本的问题上分歧在哪里？
3. 论据对比：各自使用了什么论据？谁的论据更可靠？
4. 论证严密性：谁的推理更严密？各自有什么逻辑漏洞？
5. 综合判断：有没有一个比两者都更好的第三种观点？

不需要对任何一方保持礼貌，直接指出各自的问题。
```

### 模板5：梳理总结框架（学习吸收模式）

```
你的角色是学习伙伴——帮我从这篇文章/播客里吸收知识，同时告诉我哪里不可靠。

输出结构（10 板块，通用——文章/播客/链接均适用）：

一、基础信息
- 标题/单集名称
- 作者/主播/嘉宾（姓名+身份+领域）
- 来源/平台/时间
- 内容类型（文章/播客/报道/研报等）
- 营养度：1-5星，2-3段详细理由（与模板2标准一致）
- 核心标签（3-5 个关键词）

二、一句话 + 脉络
- 一句话总结核心内容
- 内容整体脉络：开场→核心讨论→收束
- 适合谁读/听（一句话）

三、核心干货
≥5 条。每条 = 观点一句话 + 支撑论据/案例/数据（1-2句）+ 数据验证注脚（关键词联网核对 ✅/⚠️/⚡，上限 10 条）。
格式：段落叙事，三种元素自然融合，不拆成「观点→论据→✅」。

四、金句
≥5 句原文引用。格式：
1. "..." —— 为什么选这句（一句）
（播客文字稿如有时间戳，标注时间节点）

五、案例与故事
提及的典型案例/真实故事/行业事件。每条：什么事 + 论证了什么观点。
没有明显案例则跳过，不硬凑。

六、可落地的方法
（实用类内容重点；纯观点/叙事类可简化为 1-2 条或跳过）
- 可直接用的方法/技巧/步骤
- 适用场景 + 注意事项

七、我的思考
- 我认同的观点 + 为什么
- 我存疑/想继续探讨的点
- 让我联想到的（其他书/知识/现实场景/播客）

八、延伸推荐
- 相关主题的书籍/文章/播客/视频
- 作者/嘉宾提到的值得关注的人/作品/术语

九、我的行动
1-3 条。具体可操作。不是「多思考 XX」，是「去 YY 看 ZZ」。

十、总结
- 整体复盘（2-3 句）
- 营养度复核：读完 10 板块后，评级是否需要调整？
- 个人评分（1-10 分）
- 推荐理由（一句话）

风格要求：
- 克制，不啰嗦。每条信息有增量，不说「这篇文章非常精彩」这种废话。
- 聚焦「我学到了什么」。
- 案例和故事要具体，不要抽象概括。
- 营养度评级和事实核查标准与模板2一致，不降级。
```


## IMA 集成（分步执行，Python 路径优先）

分析报告生成后，按以下步骤上传到 IMA 知识库。如果凭证未配置（`~/.config/ima/client_id` 和 `~/.config/ima/api_key` 不存在），跳过全部步骤，在报告中提示用户。

**Windows 平台注意**：Git Bash 下 `node ima_api.cjs` 存在路径解析问题（`/c/Users/` → `d:\c\Users\`），且 PowerShell 5.1 传参给 Node 时中文文件名会乱码。以下步骤所有 IMA API 调用（查重、创建 media、添加知识）必须用 Python 的 `urllib.request` 执行，只有 COS 上传用 `cos-upload.cjs`（PowerShell + 纯 ASCII 参数可用）。

### 步骤 1：检查凭证

```bash
test -f ~/.config/ima/client_id && test -f ~/.config/ima/api_key && echo "CREDENTIALS_OK" || echo "NO_CREDENTIALS"
```

### 步骤 2：查重（使用 Python）

```python
import json, urllib.request, os

HOME = os.path.expanduser('~')
CLIENT_ID = open(os.path.join(HOME, '.config/ima/client_id')).read().strip()
API_KEY = open(os.path.join(HOME, '.config/ima/api_key')).read().strip()
KB_ID = '<你的知识库ID，从 IMA 控制台获取>'

def ima_post(api_path, body_dict):
    url = f'https://ima.qq.com/{api_path}'
    data = json.dumps(body_dict, ensure_ascii=False).encode('utf-8')
    req = urllib.request.Request(url, data=data, method='POST')
    req.add_header('ima-openapi-clientid', CLIENT_ID)
    req.add_header('ima-openapi-apikey', API_KEY)
    req.add_header('Content-Type', 'application/json; charset=utf-8')
    with urllib.request.urlopen(req) as resp:
        return json.loads(resp.read().decode('utf-8'))

# 查重
result = ima_post('openapi/wiki/v1/check_repeated_names', {
    'params': [{'name': '{实际文件名}', 'media_type': 7}],
    'knowledge_base_id': KB_ID
})
print(result['data']['results'][0]['is_repeated'])  # True/False
```

如已存在同名文件，在文件名后追加 `_v2`。

### 步骤 3：创建 media（使用 Python）

```python
result = ima_post('openapi/wiki/v1/create_media', {
    'file_name': '{实际文件名}',
    'file_size': {文件字节数},
    'content_type': 'text/markdown',
    'knowledge_base_id': KB_ID,
    'file_ext': 'md'
})
# 提取 media_id 和 cos_credential 各字段
media_id = result['data']['media_id']
cos = result['data']['cos_credential']
```

### 步骤 4：COS 上传（使用 cos-upload.cjs，PowerShell 执行）

注意：只能用 PowerShell 传参（确保所有参数为纯 ASCII）。中文文件路径通过 Join-Path 构建。`cos-upload.cjs` 是 IMA 知识库技能的辅助脚本，需自行准备（从 ima 知识库官方技能获取或按 COS SDK 编写）。

```powershell
$COS_UPLOAD = "<cos-upload.cjs 的绝对路径>"
$RESULT = & node $COS_UPLOAD `
  --file "$REPORT_PATH" `
  --secret-id "$SECRET_ID" --secret-key "$SECRET_KEY" --token "$TOKEN" `
  --bucket "$BUCKET" --region "$REGION" --cos-key "$COS_KEY" `
  --content-type "text/markdown" `
  --start-time "$START_TIME" --expired-time "$EXPIRED_TIME" `
  --timeout 300000
```

### 步骤 5：添加到知识库（使用 Python）

```python
FOLDER_ID = '<你的目标文件夹ID>'

result = ima_post('openapi/wiki/v1/add_knowledge', {
    'media_type': 7,
    'media_id': media_id,
    'title': '{实际文件名}',
    'knowledge_base_id': KB_ID,
    'parent_folder_id': FOLDER_ID,
    'file_info': {
        'cos_key': cos['cos_key'],
        'file_size': {文件字节数},
        'file_name': '{实际文件名}'
    }
})
print(result['code'])  # 应为 0
```

### 步骤 6：验证

在步骤 5 返回 code=0 后，确认上传成功。如果任何步骤失败，输出错误信息并在报告中标注「IMA 上传失败」。

## 避坑规则

1. **不要无脑总结，要做结构化提取**：AI 能读到原文，复述一遍是最浪费能力的用法。但「提取」不是「总结」——提取是把文章的论点、论据、逻辑链拆开摆桌面上，让后续的验证有靶子。总结是「这篇文章说的是X」，提取是「这篇文章说X，论据是Y和Z，推理走了A→B→C三步」。
2. **打破讨好语气**：直接坦诚地指出问题。"有一些值得商榷的地方" → "这里错了，原因是 X"。
3. **搜索不是搜一次**：用至少 2-3 个来源交叉验证关键数据。
4. **区分信息和框架**：信息汇总型内容 AI 时代价值递减，分析框架才有长期价值。
5. **剥掉文笔糖衣**：忽略文笔和叙事技巧，只分析实质内容。
6. **不荐股、不给出买卖建议**：财经模式下仅做分析和风险提示。

## 思维链文件（deep 档专属）

deep 档额外生成 `YYYY-MM-DD_{清洗后标题}_思维链.md`，包含：
1. 为什么选择某些搜索结果而放弃另一些
2. 逻辑审查中排除了哪些替代假设
3. 对数据来源可信度的判断依据